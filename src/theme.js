import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#bbdefb",
            light: "skyblue"
        },
        secondary: {
            main: "#15c630",
        },
        otherColor: {
            main: "#d1c4e9"
        }
    }
})
